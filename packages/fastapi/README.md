# returnz-fastapi

FastAPI + Pydantic integration for [`returnz`](https://pypi.org/project/returnz/):
`Err -> HTTPException` by error tag, `Result` / `Maybe` on the wire as tagged JSON,
and validated Pydantic settings as dependencies. Python 3.14+.
